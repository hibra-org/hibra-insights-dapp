const API_ROUTES = {
  GET_TASKS: '/api/get-tasks',
  GET_NONCE: '/api/nonce',
};

const WEB_ROUTES = {
  HOME: '/',
  OVERVIEW: '/overview',
  PORTFOLIO: '/portfolio',
  SETTING: '/setting',
};

export { API_ROUTES, WEB_ROUTES };
